# The site needs NO secrets or backend. Live music data (now-playing + top
# artists) comes straight from the public stats.fm API in the browser.
#
# Optional: override the stats.fm username (defaults to "cedric.gautier").
# VITE_STATSFM_USER=cedric.gautier
